/*
 * snap.c — window snapping / tiling
 */
#include "common.h"

void snap_apply(Window *win, SnapDir dir, int cols, int rows, int bar_h)
{
    /* TODO: snap window to edge/corner/full */
    (void)win;
    (void)dir;
    (void)cols;
    (void)rows;
    (void)bar_h;
}
